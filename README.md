#The Folder consists of 3 modules :
img_proc : Image processing and coordinate generation.

move_cmd : generating appropriate move_cmds.

path_planning : Generate path plans based on logic and problem statement.
